module terminal-lobby/t3-bridge

go 1.22

require terminal-lobby/sessionio v0.0.0

replace terminal-lobby/sessionio => ../sessionio

require terminal-lobby/slug v0.0.0

replace terminal-lobby/slug => ../slug
