[Unit]
Description=Clean old clipboard images and dropped files

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'find /tmp/clipboard-images /tmp/clipboard-files -type f -mtime +7 -delete 2>/dev/null || true'
